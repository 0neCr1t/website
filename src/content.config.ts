import { defineCollection, z } from "astro:content";
// import { glob, file } from "astro/loaders";

// Content Collections (Astro 5+): typed, Zod-validated content.
// TODO: define your collections and wire up a loader (glob for many files, file for one).
//
// const example = defineCollection({
//     loader: glob({ pattern: "**/*.json", base: "./src/content/example" }),
//     schema: z.object({
//         // TODO
//     }),
// });

export const collections = {
    // example,
};
